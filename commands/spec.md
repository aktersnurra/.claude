Read the existing SPEC.md if present, then help the user iterate on requirements.
When the user says "done", write the final version to SPEC.md and confirm.
Use extended thinking for each iteration. $ARGUMENTS
